{
  "name": "ppc-poker-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:editor":  "cd range-editor  && npm run build",
    "build:trainer": "cd poker-trainer && npm run build"
  }
}
